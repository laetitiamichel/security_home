default:
  image: ruby:3.2

pages:
  tags:
    - jekyll
  script:
    - gem install bundler
    - bundle install
    - bundle exec jekyll build -d public
  artifacts:
    paths:
      - public
  only:
    - main

